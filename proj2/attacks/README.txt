Note that Attack Alpha logs the entire cookie (ie, everything in document.cookie) to the console.

Note that in attack Echo, as per Piazza, I assume that I am the only attacker to the system (and therefore, the only person who makes a username containing "%OR username LIKE%"
